import { createSlice } from "@reduxjs/toolkit";

const initialState = {

}

const cardSlice = createSlice({
    name: 'card',
    initialState,
    reducer: {

    }
})

export default cardSlice.reducer